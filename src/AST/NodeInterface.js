class NodeInterface {
    /**
     * Compiles a node.
     *
     * @param {Compiler} compiler
     *
     * @returns
     */
    compile(compiler) { }
}

module.exports = getInterface(NodeInterface);
